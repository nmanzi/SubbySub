<div class="alert-message"><%= notice %></div>

<div class="page-header">
  <h1><%= @link.subdomain %><small>.hadric.us</small></h1>
</div>

<p>
  <b>URL:</b>
  <a href="<%= @link.url %>"><%= @link.url %></a>
</p>

<div style="text-align: right;">
	<%= link_to 'Edit', edit_link_path(@link), :class => "btn primary" %>
	<%= link_to 'Back', links_path, :class => "btn" %>
</div>